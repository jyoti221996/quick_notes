import { Routes } from '@angular/router';
import { NoteComponent } from './quick_note_project/note/note.component';

export const routes: Routes = [
     {
        path:'',
        component: NoteComponent
    },
];
